#include <iostream>
#include <algorithm>

using namespace std;

int n, arr[301];
long long MAX = 0;
int cache[301];


int main() {

	ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> arr[i];
	}

	
}